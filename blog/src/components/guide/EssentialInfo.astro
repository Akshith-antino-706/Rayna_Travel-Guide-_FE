---
interface InfoCard {
  title: string;
  description: string;
  icon: string; // 'money' | 'sim' | 'phone' | 'passport' | 'plane' | 'heart'
  color: string; // Tailwind color name
  href?: string;
}

interface Props {
  cards: InfoCard[];
  cityName: string;
}

const { cards, cityName } = Astro.props;

const bgColors: Record<string, string> = {
  green: 'bg-green-100',
  blue: 'bg-blue-100',
  red: 'bg-red-100',
  purple: 'bg-purple-100',
  cyan: 'bg-cyan-100',
  pink: 'bg-pink-100',
};

const textColors: Record<string, string> = {
  green: 'text-green-600',
  blue: 'text-blue-600',
  red: 'text-red-600',
  purple: 'text-purple-600',
  cyan: 'text-cyan-600',
  pink: 'text-pink-600',
};
---

<section class="py-8 sm:py-12">
  <div class="container-wide">
    <h2 class="text-xl sm:text-2xl font-bold text-gray-900 guide-section-header">
      Essential Information
    </h2>

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
      {cards.map((card) => {
        const Tag = card.href ? 'a' : 'div';
        return (
          <Tag
            href={card.href}
            class:list={[
              'bg-white rounded-xl p-4 sm:p-5 border border-gray-100 hover:border-orange-200 hover:shadow-sm transition-all group',
              card.href && 'cursor-pointer',
            ]}
          >
            <div class="flex items-center gap-3 mb-3">
              <div class={`w-10 h-10 ${bgColors[card.color] || 'bg-gray-100'} rounded-full flex items-center justify-center`}>
                {card.icon === 'money' && (
                  <svg class={`w-5 h-5 ${textColors[card.color]}`} fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 18.75a60.07 60.07 0 0115.797 2.101c.727.198 1.453-.342 1.453-1.096V18.75M3.75 4.5v.75A.75.75 0 013 6h-.75m0 0v-.375c0-.621.504-1.125 1.125-1.125H20.25M2.25 6v9m18-10.5v.75c0 .414.336.75.75.75h.75m-1.5-1.5h.375c.621 0 1.125.504 1.125 1.125v9.75c0 .621-.504 1.125-1.125 1.125h-.375m1.5-1.5H21a.75.75 0 00-.75.75v.75m0 0H3.75m0 0h-.375a1.125 1.125 0 01-1.125-1.125V15m1.5 1.5v-.75A.75.75 0 003 15h-.75M15 10.5a3 3 0 11-6 0 3 3 0 016 0zm3 0h.008v.008H18V10.5zm-12 0h.008v.008H6V10.5z" />
                  </svg>
                )}
                {card.icon === 'sim' && (
                  <svg class={`w-5 h-5 ${textColors[card.color]}`} fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M8.288 15.038a5.25 5.25 0 017.424 0M5.106 11.856c3.807-3.808 9.98-3.808 13.788 0M1.924 8.674c5.565-5.565 14.587-5.565 20.152 0M12.53 18.22l-.53.53-.53-.53a.75.75 0 011.06 0z" />
                  </svg>
                )}
                {card.icon === 'phone' && (
                  <svg class={`w-5 h-5 ${textColors[card.color]}`} fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 6.75c0 8.284 6.716 15 15 15h2.25a2.25 2.25 0 002.25-2.25v-1.372c0-.516-.351-.966-.852-1.091l-4.423-1.106c-.44-.11-.902.055-1.173.417l-.97 1.293c-.282.376-.769.542-1.21.38a12.035 12.035 0 01-7.143-7.143c-.162-.441.004-.928.38-1.21l1.293-.97c.363-.271.527-.734.417-1.173L6.963 3.102a1.125 1.125 0 00-1.091-.852H4.5A2.25 2.25 0 002.25 4.5v2.25z" />
                  </svg>
                )}
                {card.icon === 'passport' && (
                  <svg class={`w-5 h-5 ${textColors[card.color]}`} fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15 9h3.75M15 12h3.75M15 15h3.75M4.5 19.5h15a2.25 2.25 0 002.25-2.25V6.75A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25v10.5A2.25 2.25 0 004.5 19.5zm6-10.125a1.875 1.875 0 11-3.75 0 1.875 1.875 0 013.75 0zm1.294 6.336a6.721 6.721 0 01-3.17.789 6.721 6.721 0 01-3.168-.789 3.376 3.376 0 016.338 0z" />
                  </svg>
                )}
                {card.icon === 'plane' && (
                  <svg class={`w-5 h-5 ${textColors[card.color]}`} fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M6 12L3.269 3.126A59.768 59.768 0 0121.485 12 59.77 59.77 0 013.27 20.876L5.999 12zm0 0h7.5" />
                  </svg>
                )}
                {card.icon === 'heart' && (
                  <svg class={`w-5 h-5 ${textColors[card.color]}`} fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12z" />
                  </svg>
                )}
              </div>
              <h3 class="font-semibold text-gray-900 text-sm sm:text-base group-hover:text-orange-600 transition-colors">{card.title}</h3>
            </div>
            <p class="text-xs sm:text-sm text-gray-500 leading-relaxed">{card.description}</p>
            {card.href && (
              <p class="text-xs text-orange-500 mt-2 font-medium">Read full guide →</p>
            )}
          </Tag>
        );
      })}
    </div>
  </div>
</section>
